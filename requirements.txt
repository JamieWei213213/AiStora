# Runtime dependencies for the production image.
#
# Versions are pinned. Unpinned requirements meant that the image built today
# and the image built last month could contain different libraries, so a build
# could break or change behaviour with no commit to explain it. Development and
# test-only packages live in requirements-dev.txt and are deliberately not
# installed into the production image.
#
# Refresh with:  pip install -U -r requirements.txt && pip freeze

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Session==0.8.0
SQLAlchemy==2.0.52
alembic==1.19.1
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
blinker==1.9.0
click==8.4.2

# Session storage: redis in deployment, cachelib for the local fallback.
redis==8.1.0
cachelib==0.14.0
msgspec==0.21.1

# Database and object storage
psycopg2-binary==2.9.12
boto3==1.43.87

# Model access
google-genai==2.22.0

# Configuration and serving
python-dotenv==1.2.2
gunicorn==26.2.0
